package WhizLabsTests.practice_1;

public class P1_21 {

   static int x =50;
   public final static void main(String [] a) {

      /**
       * Compilation of the below program fails, because main method
       * is using reference variable a so it is already defined.
       * Fails as variable a is already in scope.
       */
//      Integer [] a = new Integer[2];
//      a[1] = 10;

//      for(Integer I:a)
//         System.out.print(I);
   }
}
